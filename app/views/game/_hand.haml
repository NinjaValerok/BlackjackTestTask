.row
  .col-md-12
    - if hand.user == current_user
      %strong Вы
    - else
      %strong= "Игрок: #{hand.user.email}"

.row
  .col-md-12
    %strong= "Статус: #{hand.status}"

.row
  .col-md-12
    %strong
      счет:
      %em= "#{hand.score}"
.row
  .col-md-2
    Карты:
  .col-md-9.col-md-offset-1
    - hand.cards.each do |card|
      %strong= "#{card}"