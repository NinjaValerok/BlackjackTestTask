%br
.row
  .col-md-12
    - @games.find_each do |game|
      .row
        .col-md-3
          %strong= "Игра id №: #{game.id}"
        .col-md-6
          %strong= "#{game.status}"
        .col-md-3
          - if game.status == 'неизвестно, еще не закончилась'
            =link_to 'Присоединиться', game_path(game)