$("#player_cards").html("<%= escape_javascript(render partial: 'cards', locals: { cards: @game.player_hand.cards} ) %>");
$("#player_score").html("<%= escape_javascript("#{@game.player_hand.score}") %>");

$("#dealer_cards").html("<%= escape_javascript(render partial: 'cards', locals: { cards: @game.dealer_hand.cards} ) %>");
$("#dealer_score").html("<%= escape_javascript("#{@game.dealer_hand.score}") %>");

$("#status").html("<%= @status %>");
alert("<%= @status %>");

