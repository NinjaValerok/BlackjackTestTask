$("#players").html("<%= escape_javascript(render partial: 'players', locals: { game: @game } ) %>");

$("#dealer").html("<%= escape_javascript(render partial: 'dealer' ) %>");

$("#status").html("<%= @status %>");
alert("<%= @status %>");

