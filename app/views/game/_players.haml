.row
  .col-md-12
    - @game.players.each do |player|
      .row
        .col-md-12
          %strong
            Счет игрока:
            %em= "#{player.score}"
      .row
        .col-md-2
          Карты:
        .col-md-9.col-md-offset-1
          - player.cards.each do |card|
            %strong= "#{card}"